machine:
  node:
    version: 5.0.0
deployment:
  production:
    tag: /v\d.*/
    commands:
      - ./ci/npm-login.sh
      - npm publish
  pages:
    branch: master
    commands:
      - ./ci/github_pages.sh