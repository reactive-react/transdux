machine:
  node:
    version: 5.0.0
dependencies:
  override:
    - npm install -g babel-cli    
deployment:
  production:
    tag: /v\d.*/
    commands:
      - ./ci/npm-login.sh
      - npm publish
  pages:
    branch: master
    commands:
      - ./ci/github_pages.sh